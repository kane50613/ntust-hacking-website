import { SectionTitle } from "../section-title";

export const About = () => {
  return (
    <div className="flex flex-col items-center justify-center gap-12 text-center py-16 w-full">
      <SectionTitle>關於我們</SectionTitle>
    </div>
  );
};
